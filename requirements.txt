# Python 3.11+
# Phase 0 (simulator) needs: numpy, pandas, pyarrow, pyyaml, pytest.
# lightgbm / scikit-learn / streamlit are pinned now so the environment is
# stable across phases, even though Phase 0 does not import them.
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0
PyYAML==6.0.2
pytest==8.3.4
scikit-learn==1.6.0
lightgbm==4.5.0
streamlit==1.41.1
